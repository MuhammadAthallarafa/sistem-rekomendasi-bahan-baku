<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>

  <!-- Global styles -->
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f6f8;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #2c3e50;
      color: white;
      padding: 15px 30px;
    }

    header h2 {
      margin: 0;
    }

    nav {
      margin-top: 10px;
    }

    nav a {
      color: white;
      margin-right: 15px;
      text-decoration: none;
      font-weight: bold;
    }

    main {
      padding: 30px;
    }

    /* Container */
    .container {
      background: white;
      padding: 25px;
      border-radius: 6px;
      max-width: 900px;
      margin: auto;
    }

    /* Typography */
    h3 {
      margin-bottom: 15px;
    }

    p {
      line-height: 1.6;
    }

    a {
      text-decoration: none;
    }

    /* Buttons */
    button {
      padding: 6px 12px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    button:hover {
      opacity: 0.85;
    }

    .btn-primary {
      background-color: #3498db;
      color: white;
      padding: 8px 14px;
      border-radius: 4px;
      display: inline-block;
    }

    .btn-secondary {
      background-color: #95a5a6;
      color: white;
      padding: 8px 14px;
      border-radius: 4px;
      display: inline-block;
    }

    .btn-danger {
      background-color: #e74c3c;
      color: white;
    }

    /* Form */
    .form-group {
      margin-bottom: 15px;
    }

    .form-group label {
      display: block;
      margin-bottom: 6px;
      font-weight: bold;
    }

    .form-group input {
      width: 100%;
      padding: 8px;
      border-radius: 4px;
      border: 1px solid #ccc;
      box-sizing: border-box;
    }

    form button {
      margin-right: 10px;
    }

    /* Table */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }

    table th,
    table td {
      border: 1px solid #ddd;
      padding: 10px;
      text-align: center;
    }

    table th {
      background-color: #ecf0f1;
    }

    td form {
      margin-right: 5px;
    }

    td button {
      min-width: 60px;
    }

    /* Result card */
    .result-card {
      background-color: #f9f9f9;
      padding: 20px;
      border-radius: 6px;
      border: 1px solid #ddd;
      max-width: 500px;
    }

    .result-card p {
      margin: 6px 0;
    }

    .result-card .score {
      font-size: 18px;
      font-weight: bold;
      color: #2c3e50;
    }
  </style>
</head>

<body>

  <header>
    <h2>Sistem Rekomendasi Bahan Baku</h2>
    <nav>
      <a href="/">Home</a>
      <a href="/form">Input Bahan</a>
      <a href="/materials">Daftar Bahan</a>
    </nav>
    <hr>
  </header>

  <main>
    <!-- Konten halaman akan dirender di sini -->
    <%- body %>
  </main>

</body>
</html>